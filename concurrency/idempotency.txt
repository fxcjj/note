

1. single achitecture
2. micro service achitecture


1. global unique id
a) according to business operation and content to generate a global unique id.
b) store the unique id to db or redis
c) as base micro service, some question need to concern, such as the machine
	down after write the id, so need to bring in timeout policy.
d) complex, but looks like beautiful.
	the following sulotion applies to particular situation, it's simple implementation.
	
2. dudup table
适用于在业务中有 唯一标识 的插入场景，比如在支付场景中，如果一个订单只会支付一次，所以
订单id可以作为唯一标识，这时，我们就可以建一张去重表，并且把唯一标识作为唯一索引，在我们实现时，
把创建支付单据和写入order id到去重表，放在一个事务里，如果重复创建，数据库会抛出唯一约束异常，操作就会回滚。

3. InsertOrUpdate
商品id和品类id作唯一索引

4. multi version control
适用于更新的场景


5. 状态机控制


References
https://blog.csdn.net/yuhaiyang_1/article/details/81040967



















